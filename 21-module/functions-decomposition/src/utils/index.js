const capitalize = require('./capitalize');
const getPower = require('./get-power');
const findMax = require('./find-max');

console.log(getPower(3));

module.exports = { findMax, getPower, capitalize };
